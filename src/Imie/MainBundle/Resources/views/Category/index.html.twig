{% extends "ImieMainBundle::layout_base.html.twig" %}

{% block menu %}categorie{% endblock %}
{% block body %}
    <h1>Toutes les catégories</h1>

    <table class="table table-hover">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('category_show', { 'id': entity.id }) }}">{{ entity.nom }}</a></td>
                <td>{{ entity.description }}</td>
                <td>
                  <a class="btn btn-info" href="{{ path('category_edit', { 'id': entity.id }) }}">Editer</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


            <a class="btn btn-default" href="{{ path('category_new') }}">
                Create a new entry
            </a>

    {% endblock %}
